{
  "scripts": {
    "build": "elm make --optimize src/Main.elm --output=build/elm.js",
    "deploy": "elm make --optimize src/Main.elm --output=build/elm.js && gh-pages -d ."
  },
  "devDependencies": {
    "elm-format": "^0.8.5",
    "elm-test": "^0.19.1-revision8"
  }
}